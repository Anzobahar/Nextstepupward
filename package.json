{
  "name": "nextstepupward",
  "version": "1.0.0",
  "private": true,
  "description": "NextStepUpward website + Stripe Checkout (Vercel functions / local Express)",
  "type": "module",
  "scripts": {
    "start": "node server/server.js",
    "dev": "node --watch server/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "stripe": "^16.8.0"
  }
}
